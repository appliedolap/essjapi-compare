<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<groupId>com.appliedolap.essbase</groupId>
	<artifactId>essjapi-compare</artifactId>
	<version>1.0.0-SNAPSHOT</version>

	<name>Essbase Java API Comparison Tool</name>
	<description>Compares successive versions of the Essbase Java API client jar and reports what
		changed - classes and methods added, modified, removed, or deprecated - as a single HTML
		file.</description>
	<url>https://github.com/appliedolap/essjapi-compare</url>
	<inceptionYear>2017</inceptionYear>

	<licenses>
		<license>
			<name>The Apache Software License, Version 2.0</name>
			<url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
			<distribution>repo</distribution>
		</license>
	</licenses>

	<organization>
		<name>Applied OLAP, Inc.</name>
		<url>https://www.appliedolap.com/</url>
	</organization>

	<developers>
		<developer>
			<name>Jason Jones</name>
			<organization>Applied OLAP, Inc.</organization>
			<organizationUrl>https://www.appliedolap.com/</organizationUrl>
		</developer>
	</developers>

	<scm>
		<connection>scm:git:https://github.com/appliedolap/essjapi-compare.git</connection>
		<developerConnection>scm:git:https://github.com/appliedolap/essjapi-compare.git</developerConnection>
		<url>https://github.com/appliedolap/essjapi-compare</url>
	</scm>

	<properties>
		<maven.compiler.release>17</maven.compiler.release>
		<project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
	</properties>

	<dependencies>
		<dependency>
			<groupId>com.github.siom79.japicmp</groupId>
			<artifactId>japicmp</artifactId>
			<version>0.26.2</version>
		</dependency>
		<dependency>
			<groupId>info.picocli</groupId>
			<artifactId>picocli</artifactId>
			<version>4.7.7</version>
		</dependency>
		<dependency>
			<groupId>org.junit.jupiter</groupId>
			<artifactId>junit-jupiter</artifactId>
			<version>6.1.3</version>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.slf4j</groupId>
			<artifactId>slf4j-api</artifactId>
			<version>2.0.19</version>
		</dependency>
		<dependency>
			<groupId>org.thymeleaf</groupId>
			<artifactId>thymeleaf</artifactId>
			<version>3.1.5.RELEASE</version>
		</dependency>
		<dependency>
			<groupId>ch.qos.logback</groupId>
			<artifactId>logback-classic</artifactId>
			<version>1.6.3</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-javadoc-plugin</artifactId>
				<version>3.11.2</version>
				<configuration>
					<!--
						all,-missing rather than all. Every class, method and field here carries a
						description, which is the part worth having. What -missing additionally
						demands is an @return tag beneath a one-line accessor description, and
						"@return the base folder" directly below "The base folder." is noise
						rather than documentation. Everything doclint catches that is genuinely
						wrong - malformed HTML, a broken @link, a tag naming a parameter that does
						not exist - still fails the build.
					-->
					<doclint>all,-missing</doclint>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-jar-plugin</artifactId>
				<version>3.4.2</version>
				<configuration>
					<archive>
						<manifest>
							<!-- Puts Implementation-Version in the manifest, which is where
							     the version flag reads it from. -->
							<addDefaultImplementationEntries>true</addDefaultImplementationEntries>
						</manifest>
					</archive>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-shade-plugin</artifactId>
				<version>3.6.0</version>
				<executions>
					<execution>
						<phase>package</phase>
						<goals>
							<goal>shade</goal>
						</goals>
						<configuration>
							<!-- A second, runnable artifact rather than replacing the plain jar. -->
							<shadedArtifactAttached>true</shadedArtifactAttached>
							<!-- Only meaningful when the shaded jar is the published artifact,
							     which it is not; without this it litters the project root. -->
							<createDependencyReducedPom>false</createDependencyReducedPom>
							<shadedClassifierName>cli</shadedClassifierName>
							<transformers>
								<transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
									<mainClass>com.appliedolap.essjapicompare.JapiCompare</mainClass>
								</transformer>
								<!-- japicmp brings JAXB, which needs its service files merged. -->
								<transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer" />
							</transformers>
							<filters>
								<filter>
									<artifact>*:*</artifact>
									<excludes>
										<exclude>META-INF/*.SF</exclude>
										<exclude>META-INF/*.DSA</exclude>
										<exclude>META-INF/*.RSA</exclude>
										<exclude>module-info.class</exclude>
									</excludes>
								</filter>
							</filters>
						</configuration>
					</execution>
				</executions>
			</plugin>
		</plugins>
	</build>
</project>