# Normalize text in the repository; Git picks the working-tree ending per platform.
* text=auto

# Shell scripts are executed as checked out, so their endings are functional: a shebang
# followed by a carriage return is not a shebang.
*.sh text eol=lf

# Binary, never touched.
*.jar   binary
*.png   binary
*.ico   binary
*.pdf   binary
